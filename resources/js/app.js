import './bootstrap.js';

// If you want to auto-init the client globally for SPA, you can expose it here:
// import TransferNotificationClient from './pusher-client.js';
// window.notificationClient = new TransferNotificationClient(window.__USER_ID__, window.__CSRF__);

import './bootstrap';
